interface:
  display_name: "Send Email"
  short_description: "Draft and send wzrdmail email"
  default_prompt: "Use $send-email to prepare and send a wzrdmail message safely."
dependencies:
  tools:
    - type: "mcp"
      value: "wzrdmail"
      description: "wzrdmail hosted MCP server"
      transport: "streamable_http"
      url: "https://mcp.mail.wzrd.tech/mcp"
policy:
  allow_implicit_invocation: true
